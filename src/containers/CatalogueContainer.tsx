import React, { ReactElement } from 'react';

// Components

// Styles
import styles from './CatalogueContainer.module.scss';

const CatalogueContainer: React.FC = (): ReactElement => {
	return <div className={styles.catalogueContainer}>CatalogueContainer</div>;
};

export default CatalogueContainer;
